// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.50.0';
export const APP_VERSION_NOTE = '[Dịch Card] Bảng "Sức khoẻ thẻ" nay BẤM ĐƯỢC: click 1 vấn đề là NHẢY THẲNG tới đúng trường — trường thường thì tự chọn tab + cuộn tới + tô sáng 2.6s; trường REGEX thì mở panel Quản lý Regex + chọn đúng script chứa nó. Phủ đủ cả 2 nơi (bảng field ảo hoá + view regex tách riêng).';
