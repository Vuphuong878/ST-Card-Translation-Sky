// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.22.0';
export const APP_VERSION_NOTE = '[Mod Card] Phase 2+3: Chế độ MỞ RỘNG/đào sâu (đọc toàn cảnh lorebook → bổ sung 3-4 phần, viết chi tiết, bám lore; chọn mức nhẹ/vừa/sâu) + panel "Đào sâu 1 phần" (mở rộng đúng 1 sub-block như <Appearance>). Đổi output mod script/narrative từ JSON sang XML → hết vỡ với card bự.';
