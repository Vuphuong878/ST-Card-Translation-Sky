// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.40.1';
export const APP_VERSION_NOTE = '[Tạo Card] (#9.4) Tạo thẻ từ truyện nay có nút "■ Dừng" — bấm là HUỶ THẬT mọi call AI đang quét/tạo (truyền AbortSignal xuống fetch), không còn chạy nền. (#9.3) Input/kết quả trang này vốn đã tự lưu (đổi tab/F5 không mất) nên giữ nguyên.';
