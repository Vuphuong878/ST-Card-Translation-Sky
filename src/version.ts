// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.36.0';
export const APP_VERSION_NOTE = '[Trích Card] Bước 3 thêm 3 tính năng (theo client): (1) SẮP XẾP danh sách nhân vật — Số lần xuất hiện / Tên A→Z / Vai (Chính→Phụ→NPC). (2) QUÉT TRÙNG LẶP tự động: gộp nhanh trùng hiển nhiên (cùng tên gốc/tên Việt/biệt danh, không tốn API) + AI nhận diện cùng-người dù tên khác hẳn (biệt danh) → GỢI Ý nhóm để bạn duyệt (nút "Gộp nhóm"/"Gộp tất cả"/"Bỏ qua"). Gộp giữ vai/giới cao nhất, cộng số lần xuất hiện, có nút Tách hoàn tác.';
