// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.64.0';
export const APP_VERSION_NOTE = 'MỚI: nút "🚀 Dịch siêu tốc" (cạnh Dịch nhẹ / Dịch đầy đủ) — gom entry thông minh kiểu bin-packing: entry NGẮN dồn chung 1 call API (đi model PHỤ/flash cho nhanh, tối đa 12 entry & 10K ký tự/lô), entry DÀI (>8K) để riêng (đi model CHÍNH/pro). Giảm mạnh số call, vẫn đa luồng qua pool; nhóm schema MVU (initvar/controller) vẫn dịch 1-1 an toàn. | MỚI: theo dõi LANE LỖI — call fail (429/5xx/timeout, proxy chung nghẽn ngoài RPM) thì lane đó tự NGHỈ 15 GIÂY (call dồn sang lane khác), panel luồng hiện huy hiệu "⚠ lỗi ×N"; fail ≥5 lần liên tiếp → tên model tô ĐỎ đậm; call thành công tự reset. | 1.63: sửa bug #3 — dịch tới TavernHelper 148KB rồi "nằm im" (guard schema re-dịch cả field vì 1 chữ Hán; nay theo tỷ lệ + lưới an toàn giới hạn retry). | 1.62: nút 🐞 Báo lỗi ở header. | 1.61+1.60: sửa Kiểm tra lỗi dịch báo oan. | 1.59: retry đẩy xuống model phụ. | 1.58: bộ gác chữ Hán sót chống DONE giả.';
