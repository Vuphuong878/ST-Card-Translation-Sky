// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.40.2';
export const APP_VERSION_NOTE = '[Dịch Card] (#8b) Log dễ đọc hơn cho người không rành kỹ thuật: bộ "AI tự sửa lỗi" khi bỏ một bản sửa nay ghi rõ "🛡️ Giữ bản gốc — bản AI sửa còn nhiều lỗi hơn, không áp dụng để khỏi hỏng thẻ" (trước là "❌ Rejected... worsened severity score" khó hiểu). Việt hoá + làm rõ toàn bộ log dịch (Đang dịch/Xong lô/Đã dừng/Tiếp tục/Chiến lược B-C/Đối chiếu…).';
