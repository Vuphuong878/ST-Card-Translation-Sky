// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.19.2';
export const APP_VERSION_NOTE = '[Dịch Card] Nút "Xem/chọn trường (bỏ dịch)" — liệt kê tất cả trường TRƯỚC khi dịch để bỏ tick những trường muốn tự dịch tay (không cần Start rồi Pause). Bỏ tick = IGNORED = không gọi API, gõ bản dịch thẳng vào ô. Kèm hint hướng dẫn.';
