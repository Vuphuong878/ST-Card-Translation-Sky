// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.47.2';
export const APP_VERSION_NOTE = '[Nội bộ] Bổ sung test cho hàm trích JSON Patch (extractJsonPatches/hasJsonPatchOps) — nốt phần test lõi còn thiếu (nay 49 test). Gitignore các file thẻ-test/fixture trong dev_data/ (dùng local cho test surgical, không commit). KHÔNG đổi hành vi app.';
