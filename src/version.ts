// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.35.0';
export const APP_VERSION_NOTE = '[Mod Card] Mod RIÊNG Lorebook: nay có thể import thẳng 1 file Lorebook (JSON có "entries") → mod (mở rộng / rule / prompt tùy chỉnh) → xuất RIÊNG Lorebook (giữ đúng format gốc), không cần cả thẻ — tránh lag khi add cả card vào ST. Tự nhận diện card hay lorebook khi tải; đủ chức năng mod trên entries.';
