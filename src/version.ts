// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.36.5';
export const APP_VERSION_NOTE = '[Dịch Card] Tải thẻ qua link Discord: tự chuẩn hoá link "media.discordapp.net" (proxy trả ảnh WEBP đã resize → MẤT dữ liệu thẻ nhúng trong PNG) về "cdn.discordapp.com" ảnh gốc, bỏ format/width/height, giữ chữ ký ex/is/hm. Nay dán link ảnh thẻ từ Discord tải được đầy đủ thẻ (đã test: card v3, 307 lorebook, 939 field).';
