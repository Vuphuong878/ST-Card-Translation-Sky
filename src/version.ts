// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.54.3';
export const APP_VERSION_NOTE = '[Nội bộ · Game UI Studio 1/3] Thêm bộ kiểm regex 4 tầng cho Tạo Card (nền cho Game UI mới dạng chat): V1 cú pháp (findRegex compile, JS parse, enum), V2 MATCH THẬT (findRegex phải khớp đoạn văn mẫu + đủ nhóm $1..$9), V4 khớp schema (chống bịa biến). Đây là "chứng minh regex ăn trước khi giao". Chưa đổi giao diện — mới là lõi + 19 test. UI chat sẽ tới ở bản kế.';
