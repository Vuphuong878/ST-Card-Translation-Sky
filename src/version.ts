// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.19.1';
export const APP_VERSION_NOTE = '[Dịch Card] Panel "Luồng đang chạy" hiện thêm % hoàn thành tổng + badge PROVIDER (Gemini/OpenAI/Claude) cho mỗi call — biết provider nào đang chạy entry nào. (Tick bỏ dịch từng field vốn đã có sẵn ở bảng Field Editor: bỏ tick / nút ⊘ = bỏ dịch + sửa tay.)';
