// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.23.1';
export const APP_VERSION_NOTE = '[Dịch Card] Redesign UI Provider bổ sung cho gọn: mỗi provider có nút "🔄 Load model" RIÊNG (quét model của provider đó → gợi ý vào ô model), bố cục 2 field/hàng (Loại|Base URL, Model|RPM) tiết kiệm diện tích, header bật/tắt gọn.';
