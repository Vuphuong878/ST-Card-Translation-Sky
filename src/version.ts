// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.12.2';
export const APP_VERSION_NOTE = '[Tạo Card] Chạy nhiều luồng chồng theo RPM (Pro ~5, Flash ~17): đổi RPM limiter sang kiểu "chốt giờ bắt đầu" cho request overlap; nâng Batch song song lên 24.';
