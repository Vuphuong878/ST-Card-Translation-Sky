// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.53.0';
export const APP_VERSION_NOTE = '[Dịch Card] Nâng cấp RAG — NGỮ CẢNH RỘNG/SÂU HƠN: (1) nay tự KÍCH HOẠT entry Lorebook theo KEYWORD — dịch đoạn nào nhắc tới keyword của 1 entry khác (đã dịch) thì kéo nội dung entry đó vào ngữ cảnh (giống cách SillyTavern trigger lorebook) → tên/thuật ngữ/định nghĩa lore nhất quán hơn. (2) nới ngân sách ngữ cảnh cho tường thuật + lorebook (thêm vài field liên quan/lần dịch). Vẫn client-side, không tốn call API. Thêm 6 test.';
