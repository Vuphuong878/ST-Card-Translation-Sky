// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.49.0';
export const APP_VERSION_NOTE = '[Đồng bộ 5 app] (1) Tạo Preset — thêm nút ⏹ Dừng cho trợ lý chat: bấm là HỦY câu trả lời đang chạy (trước chỉ có timeout). (2) Tạo Card — game HTML sinh ra nay được kiểm cú pháp JS, hiện badge đỏ "N/M script vỡ JS" nếu có (bổ sung cho autoFixGameHtml vốn chỉ sửa cấu trúc). Tới đây cả 5 app đều: nút Dừng cắt được việc đang chạy + có lưới an toàn cú pháp script ở nơi cần.';
