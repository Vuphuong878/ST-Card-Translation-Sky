// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.42.3';
export const APP_VERSION_NOTE = '[Toàn bộ] Tăng cỡ chữ nền của cả 5 app từ 14px → 16px (dùng rem nên mọi chữ to theo ~14%) để dễ đọc hơn, khỏi phải zoom Chrome 125%. Nếu vẫn muốn to/nhỏ hơn cứ báo để chỉnh (17-18px).';
