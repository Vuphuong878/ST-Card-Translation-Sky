// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.19.0';
export const APP_VERSION_NOTE = '[Dịch Card] Chiến Lược C (EJS) giờ chạy ĐA LUỒNG RPM như MVU: chia lô nhỏ (50 mục/call) + bắn song song (callProvider tự gate RPM + xoay key) + TỰ RETRY item bị sót. Sửa bug card EJS bự (nghìn keyword) bị cắt output → sót → phải chạy 2-3 lần. Nhanh hơn nhiều, không còn bỏ sót.';
