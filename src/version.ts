// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.24.0';
export const APP_VERSION_NOTE = '[Tạo Card] Đa provider chạy song song (như Dịch Card): tick "🔀 Gộp vào POOL đa-provider" ở ≥2 profile (Cài đặt) → engine rải call round-robin nhiều provider cùng lúc (mỗi profile giữ đa-key + RPM riêng, rate-limit tách theo provider). 1 profile = như cũ. (Preset/Mod/Trích Card làm tiếp.)';
