// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.14.0';
export const APP_VERSION_NOTE = '[Tạo Card] "Tạo thẻ từ truyện" đủ 4 nhóm: (1) chunk quét truyện dài + gộp roster, (2) chọn NHIỀU nhân vật → tạo thẻ song song + "Gộp mục đã chọn", (3) thay {{user}} + thiết lập bổ sung, (4) mẫu thẻ/chia giai đoạn/lược trường/tự viết tiếp/world entries + lọc entry vụn vặt.';
