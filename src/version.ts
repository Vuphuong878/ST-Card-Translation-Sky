// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.51.0';
export const APP_VERSION_NOTE = '[Dịch Card] Nhật ký nay GOM THEO GIAI ĐOẠN: Chuẩn bị → Dịch → Kiểm tra, mỗi nhóm gấp/mở được (bấm tiêu đề) cho gọn — thay vì một dòng chảy phẳng. Bộ lọc theo loại (✓/✗/!/↻…) vẫn dùng bình thường. Ca đơn giản (một giai đoạn) vẫn hiện phẳng như cũ.';
