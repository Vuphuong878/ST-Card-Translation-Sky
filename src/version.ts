// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.32.1';
export const APP_VERSION_NOTE = '[Trích Card] Nâng cấp "Kèm mô tả danh tính khi quét" (bước 3): mỗi nhân vật nay kèm GIỚI TÍNH (♂/♀) + phân loại VAI (★ Chính / Phụ / NPC-người qua đường) hiển thị badge ngay trên thẻ — biết ngay ai chính ai phụ để quyết định tạo chi tiết hay đơn giản, không cần đọc truyện. Merge nhiều đoạn lấy vai cao nhất.';
