// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.36.3';
export const APP_VERSION_NOTE = '[Trích Card] Thêm nút "🗑 Xóa / Làm lại" ở mục 4 (Tạo thẻ) và mục 5 (Lorebook). Trước đây bấm tạo lại nó chỉ "tiếp tục, bỏ qua mục đã tạo" nên cứ hiện kết quả cũ; nay bấm Xóa để reset trạng thái + kết quả và TẠO LẠI TỪ ĐẦU.';
