// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.12.3';
export const APP_VERSION_NOTE = '[Tạo Card] Học từ 小玉写卡器: nút "Persona theo giai đoạn" ở ô Mô tả — sinh persona chia 3 giai đoạn quan hệ (sơ giao/quen/thân) + xuyên suốt, output có cấu trúc tag.';
