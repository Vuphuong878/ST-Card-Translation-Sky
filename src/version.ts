// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.20.0';
export const APP_VERSION_NOTE = '[Dịch Card] ĐA PROVIDER — cấu hình nhiều provider (mỗi cái bộ key riêng + model chính/phụ + RPM sửa tự do + ngưỡng token), engine rải call ĐỀU chạy SONG SONG (mỗi provider RPM+xoay key độc lập, không đụng nhau) → nhanh hơn nhiều. Chất lượng giữ như 1 provider (covariance/RAG không phụ thuộc provider). Panel luồng chạy hiện RPM tách theo provider. Lưu tự động.';
