// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.21.0';
export const APP_VERSION_NOTE = '[Mod Card] Chế độ "Mod biến MVU-Zod" (Phase 1): gom biến schema → AI đổi tên/nghĩa theo yêu cầu (output XML cho chắc) → duyệt bảng old→new → áp ĐỒNG BỘ khắp schema/getvar/initvar/mvu_update (deterministic, không đụng văn xuôi & runtime MVU).';
