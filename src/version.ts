// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.17.0';
export const APP_VERSION_NOTE = '[Giao diện] Làm SÁNG chữ mờ (muted) cho dễ đọc ở hub/tao-card/preset/novalcard + PHÓNG TO chrome nhỏ: header, rail (emoji+nhãn), thanh toolbar iframe & nút Tải lại/Tab mới, nút Cập nhật.';
