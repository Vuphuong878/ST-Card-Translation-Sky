// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.55.2';
export const APP_VERSION_NOTE = 'SỬA UI panel "Kiểm Tra Lỗi Dịch" (Dịch Card): danh sách lỗi bị set cứng chiều cao (ô cuộn 300-450px lồng trong trang) → cụt, khó cuộn, khó đọc. Nay danh sách lỗi CHẢY TỰ NHIÊN theo trang (cuộn 1 lần, đọc hết) cho cả 4 danh sách (Quét Regex, kết quả sửa regex, lỗi field, lỗi thẻ, biến mồ côi). Ô xem ORIGINAL/CURRENT khi bung lỗi tăng từ 80px → 260px cho dễ đọc nội dung bug.';
