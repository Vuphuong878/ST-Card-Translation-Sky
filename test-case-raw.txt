<USER_REQUEST>
app dịch regex đang lỗi đây là bản gốc:```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Ma+Shan+Zheng&family=Noto+Serif+SC:wght@400;700&family=Zhi+Mang+Xing&display=swap');
    :root {
      --rod-dark: #1f1208;
      --rod-med: #3e2814;
      --rod-light: #5c3c1e;
      --gold-dark: #8b6914;
      --gold-med: #c09a30;
      --gold-light: #f0d060;
      --red-seal: #a82222;
      --paper-bg: #14100c;
      --paper-tex: #1f1913;
      --text-main: #d4c5a0;
      --text-muted: #8a7a58;
    }
    * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
    html, body {
      margin: 0;
      padding: 0;
      background: transparent;
      font-family: "Noto Serif SC", serif;
      color: var(--text-main);
      font-size: 12px;
      height: auto;
      overflow: hidden;
    }
    body {
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 4px;
    }
    /* ═══════ 卷轴外壳 ═══════ */
    #app {
      width: 100%;
      max-width: 480px;
      display: flex;
      flex-direction: column;
      position: relative;
      filter: drop-shadow(商 10px 20px rgba(0,0,0,0.8));
      border-radius: 4px;
      overflow: hidden;
      height: 650px;
      transition: height 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    }
    #app.collapsed { height: 44px !important; }
    /* ═══════ 卷轴画杆 (天杆 & 地杆) ═══════ */
    .scroll-rod {
      height: 44px;
      background: linear-gradient(to bottom, var(--rod-dark) 0%, var(--rod-med) 30%, var(--rod-light) 50%, var(--rod-med) 70%, var(--rod-dark) 100%);
      position: relative;
      z-index: 10;
      border-top: 1px solid rgba(255,255,255,0.1);
      border-bottom: 1px solid rgba(0,0,0,0.8);
      display: flex;
      align-item
<truncated 45216 bytes>
{});
      const bag = pro.物品栏 || {};
      const muGu = _.get(pro, '本命蛊.极乐噬阴母蛊', {});
      const derivatives = pro.蛊 || {};
      let html = '';
      html += '<div class="category-title" style="border-color:#d070d0;color:#d070d0;">本命母蛊</div>';
      if (Object.keys(muGu).length > 0) {
        html += `<div class="paper-block">
          <div class="block-title" style="color:#d070d0; margin-bottom:8px;">极乐噬阴母蛊 <span class="status-tag" style="border-color:#c040c0;color:#e080e0;background:rgba(180,40,180,0.1); margin-top:0; margin-left:6px;">${muGu.当前状态 || '未知'}</span></div>
          <div style="font-size:11px; color:#d4c5a0; line-height:1.5;">${muGu.当前行为 || '无动静'}</div>
        </div>`;
      } else {
        html += '<div class="empty-state" style="padding: 10px 0; font-size: 14px;">母蛊未觉醒</div>';
      }
      html += '<div class="category-title" style="border-color:#e06060;color:#e0a0a0;">其他蛊虫</div>';
      if (Object.keys(derivatives).length > 0) {
        Object.keys(derivatives).forEach(k => {
          const item = derivatives[k];
          const desc = typeof item === 'object' ? item.描述 || '' : '';
          const rank = typeof item === 'object' ? item.品阶 || '蛊' : '蛊';
          const count = Math.floor(Number(typeof item === 'object' ? item.数量 || 0 : item || 0));
          html += `<div class="bamboo-slip" style="cursor:default;"><div class="joint"></div><div class="bs-left"><div class="bs-name"><span class="seal-small" style="color:#ff9090;border-color:#a82222;">${rank}</span> ${k}</div><div class="bs-sub">${desc}</div></div><div class="bs-right" style="font-family:'Ma Shan Zheng',serif;font-size:18px;color:var(--gold-med);">×${count}</div></div>`;
        });
      } else {
        html += '<div class="empty-state" style="padding: 10px 0; 
<truncated 124342 bytes>

NOTE: The output was truncated because it was too long. Use a more targeted query or a smaller range to get the information you need.